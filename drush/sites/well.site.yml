local:
    root: /var/www/well/docroot
    uri: 'http://local.well.com'
    host: local.well.com
    user: vagrant
    ssh: { options: '-o PasswordAuthentication=no -i $HOME/.vagrant.d/insecure_private_key' }
